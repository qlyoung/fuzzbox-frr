version: '3.3'

services:
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"

  influx:
    image: influxdb:latest
    ports:
      - "8086:8086"
    environment:
      - "INFLUXDB_DB=fuzzing"
  #fuzz-bgpd:
  #  image: qlyoung/fuzzbox-frr
  #  build:
  #    context: .
  #  command: ["bgpd", "2"]
  #  deploy:
  #    resources:
  #      reservations:
  #        cpus: '2'
  fuzz-zebra:
    image: qlyoung/fuzzbox-frr
    build:
      context: .
    command: ["zebra", "2"]
    deploy:
      resources:
        reservations:
          cpus: '2'
  fuzz-pimd:
    image: qlyoung/fuzzbox-frr
    build:
      context: .
    command: ["pimd", "2"]
    deploy:
      resources:
        reservations:
          cpus: '2'
  #fuzz-ospfd:
  #  image: qlyoung/fuzzbox-frr
  #  build:
  #    context: .
  #  command: ["ospfd", "2"]
  #  deploy:
  #    resources:
  #      reservations:
  #        cpus: '2'
